server:
  port: 9090
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:default
    username: username
    password: password

custom:
  datasource:
    enabled: true

debug: true